# Sri Rajan Photography — Pass 2

Added:
- `/portfolio` editorial project grid
- reusable `/stories/[slug]` wedding-story pages
- `/services` full services page
- `/albums` albums and frames showcase
- `/contact` responsive enquiry form prototype
- shared responsive navigation/footer component for inner pages
- expanded responsive styles for desktop/mobile

Media remains placeholder-first by design. Replace placeholder blocks with real studio photos/video in the next content pass.

Run locally:
1. `npm install`
2. `npm run dev`
3. Open `http://localhost:3000`
